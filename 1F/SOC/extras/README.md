# 1F SOC Extras

This folder holds one-off or older MATLAB plotting scripts.

The main SOC scripts live one level up:

- `SOCPlot.m`
- `SOCFitPlot.m`

`PlottingBasic.m` is kept as an older basic plotting check. It now resolves data
paths relative to the parent `SOC` folder.
